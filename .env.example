# Override the path to the global treepad config file.
# Default resolution: $XDG_CONFIG_HOME/treepad/config.json → ~/.config/treepad/config.json
TREEPAD_CONFIG=

# Override the XDG base directory for config files.
# Default: ~/.config
XDG_CONFIG_HOME=
